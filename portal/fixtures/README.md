# Fictional test fixtures

Every identity and identifier in this directory is synthetic and uses the reserved `.test` domain or an explicit `.test` suffix. No client document, credential, authenticator, bank coordinate, SSN, tax return, browser profile, or production object is included. Object fixtures exercise parsers and upload controls only.
